# ui — Trading

Last updated 2026-07-15. **N/A — no user-facing product frontend.**

The only UI is the internal **Streamlit dashboard** (`web.py`,
http://localhost:8501, `TradingDashboard` task) — functional/operational, not a
designed product surface, so it carries no visual-design standard. Record and
daily-report HTML are SCRIPT-GENERATED (never hand-styled — see tooling.md).

If a real product UI is ever built, replace this stub with its standards.
