# ui / ux — Trading

Last updated 2026-07-15. **N/A — no user-facing product UI or UX.**

The only interface is the internal **Streamlit dashboard** (`web.py`,
http://localhost:8501, `TradingDashboard` task) — functional/operational, not a
designed product surface, so it carries no visual-design or UX-flow standard.
Record and daily-report HTML are SCRIPT-GENERATED (never hand-styled — see
tooling.md). There is no end-user flow to design: the "users" are Evan + the
scheduled automation.

If a real product UI/UX is ever built, replace this stub with its standards.
