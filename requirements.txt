# DIRECT dependencies, pinned to the exact versions in the working .venv
# (audit 2026-07-17, record CG). Floors were replaced with pins because yfinance
# in particular is semantically load-bearing: price_cache is split-adjusted,
# dividend-UNadjusted (auto_adjust=False), and a silent yfinance upgrade could
# change adjustment behavior. Upgrade deliberately: bump the pin, re-run the
# frozen regression tests (d=+/-0.0000pp), spot-check a known dividend payer's
# close vs cache.
#
# THIS FILE ALONE DOES NOT REPRODUCE THE ENVIRONMENT (audit 2026-08-05, finding
# 21). It pins 9 packages; 97 are installed. yfinance's own dependency chain --
# curl_cffi, requests, urllib3, lxml, beautifulsoup4, peewee, frozendict,
# multitasking, platformdirs -- was entirely unpinned, and those are exactly the
# libraries that parse and shape the prices this project treats as ground truth.
# A clean rebuild could therefore land a different adjustment behaviour behind an
# unchanged yfinance pin.
#
# `requirements.lock.txt` (generated 2026-08-05 by `pip freeze`, 96 packages) is
# the full transitive snapshot of the KNOWN-GOOD venv. Rebuild from the lock:
#     .venv\Scripts\python.exe -m pip install -r requirements.lock.txt
# Keep both: this file says what the project asks for, the lock says what it was
# last verified against. Regenerate the lock after any deliberate upgrade, in the
# same commit as the frozen-test output that cleared it.
httpx==0.28.1
markdown==3.10.2
numpy==2.4.4
pandas==3.0.2
plotly==6.7.0
rich==15.0.0
streamlit==1.57.0
watchdog==6.0.0
yfinance==1.3.0
