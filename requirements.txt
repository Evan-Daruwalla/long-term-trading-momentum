# Pinned to the exact versions in the working .venv (audit 2026-07-17, record CG).
# Floors were replaced with pins because yfinance in particular is semantically
# load-bearing: price_cache is split-adjusted, dividend-UNadjusted
# (auto_adjust=False), and a silent yfinance upgrade could change adjustment
# behavior. Upgrade deliberately: bump the pin, re-run the frozen regression
# tests (d=+/-0.0000pp), spot-check a known dividend payer's close vs cache.
httpx==0.28.1
numpy==2.4.4
pandas==3.0.2
plotly==6.7.0
rich==15.0.0
streamlit==1.57.0
yfinance==1.3.0
